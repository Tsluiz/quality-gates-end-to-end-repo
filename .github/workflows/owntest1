name: Own pipeline try

on:
  pull_request:
    types: ['opened', 'edited', 'reopened', 'synchronize']

  workflow_dispatch:
    inputs:
      sut:
        description: 'Try own pipeline'
        required: true
